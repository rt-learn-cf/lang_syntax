<cfscript>
    writeLine("Replace String Examples");
    writeLine("Replace all occurrence");
    writeLine("replace('apple', 'p', 'd') = #replace('apple', 'p', 'd')#");

    writeLine("String Concatenation");
    writeLine("Hello" & "World");

    writeOutput("<hr>");
    writeLine("Character at position");
    writeLine("Hello"[1]);
</cfscript>
